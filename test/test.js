'use strict';

var assert = require('assert');
var removeHtmlExtension = require('..');

describe('remove-html-extension', function() {

  it('should do something awesome', function() {
    assert.equal(removeHtmlExtension(), true);
  });
});
